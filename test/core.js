import { expect } from 'chai'
import { Map } from 'immutable'

describe('boilerplate app', () => {

	describe('Test', () => {
		it('should run tests', () => {
			expect(1).to.equal(1);
		});
	});
	
});